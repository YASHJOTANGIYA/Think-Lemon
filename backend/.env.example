# MongoDB Atlas Configuration
# Replace the values below with your actual MongoDB Atlas credentials

# MongoDB Atlas Connection String
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
# Example: mongodb+srv://thinklemon_admin:yourpassword@cluster0.xxxxx.mongodb.net/think-lemon?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/think-lemon?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# JWT Secret (Change this to a random secure string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Environment
NODE_ENV=development

# Shiprocket Configuration
SHIPROCKET_EMAIL=your_shiprocket_email
SHIPROCKET_PASSWORD=your_shiprocket_password

# Razorpay Configuration
RAZORPAY_KEY_ID=rzp_test_placeholder_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# ============================================
# INSTRUCTIONS TO UPDATE YOUR .env FILE:
# ============================================
# 
# 1. Open backend/.env file
# 2. Replace the MONGODB_URI line with your actual MongoDB Atlas connection string
# 3. Make sure to replace:
#    - <username> with your database username
#    - <password> with your database password
#    - <cluster-url> with your cluster URL (e.g., cluster0.xxxxx.mongodb.net)
# 4. Save the file
# 5. Restart the backend server (it will auto-restart if using nodemon)
